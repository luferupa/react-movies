import {Center, Text} from 'native-base'

const Loading = () => {
    return (
        <Center>
            <Text fontWeight='bold'>Loading results</Text>
        </Center>
    )
}

export default Loading